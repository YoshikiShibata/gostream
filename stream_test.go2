// Copyright © 2020 Yoshiki Shibata. All rights reserved.

package gostream

import (
	"math/rand"
	"os"
	"slices"
	"sort"
	"strconv"
	"sync"
	"testing"
	"time"
)

func TestStream_ForEach(t *testing.T) {
	defer trace("TestStream_ForEach")()

	data := make([]int, 1000)
	for i := 0; i < len(data); i++ {
		data[i] = i
	}

	t.Run("serial", func(t *testing.T) {
		defer trace("TestStream_ForEach/serial")()

		want := 0
		Of(data...).ForEach(func(v int) {
			if v != want {
				t.Fatalf("t is %d, want %d", v, want)
			}
			want++
		})
	})

	t.Run("parallel", func(t *testing.T) {
		defer trace("TestStream_ForEach/parallel")()

		var result []int
		var lock sync.Mutex

		Of(data...).Parallel().ForEach(func(v int) {
			lock.Lock()
			result = append(result, v)
			lock.Unlock()
		})

		lock.Lock()
		defer lock.Unlock()
		if len(result) != len(data) {
			t.Errorf("len(result) is %d, want %d",
				len(result), len(data))
		}

		isSorted := func() bool {
			for i := 1; i < len(result); i++ {
				if result[i-1] > result[i] {
					return false
				}
			}
			return true
		}
		if goMaxProcs != 1 && isSorted() {
			t.Errorf("result(%v) is sorted\n", result)
		}
	})
}

func TestStream_Filter(t *testing.T) {
	defer trace("TestStream_Filter")()

	for _, tc := range [...]struct {
		dataSize int
	}{
		{dataSize: 0},
		{dataSize: 1},
		{dataSize: 1000},
	} {
		var input []int
		var want []int

		for i := 0; i < tc.dataSize; i++ {
			input = append(input, i)
			if i&1 == 1 {
				want = append(want, i)
			}
		}

		ds := strconv.Itoa(tc.dataSize)

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_Filter/serial:" + ds)()

			result := Of(input...).Filter(func(t int) bool {
				return t&1 == 1
			}).ToSlice()

			if !slices.Equal(result, want) {
				t.Errorf("result is %v, want %v", result, want)
			}
		})

		t.Run("parallel", func(t *testing.T) {
			defer trace("TestStream_Filter/parallel:" + ds)()

			result := Of(input...).Parallel().Filter(func(t int) bool {
				return t&1 == 1
			}).ToSlice()

			if !slices.Equal(result, want) {
				t.Errorf("result is %v, want %v", result, want)
			}
		})
	}
}

func TestStream_Map(t *testing.T) {
	defer trace("TestStream_Map")()

	for _, tc := range [...]struct {
		dataSize int
	}{
		{dataSize: 0},
		{dataSize: 1},
		{dataSize: 1000},
	} {
		var input []int
		var want []string

		for i := 0; i < tc.dataSize; i++ {
			input = append(input, i)
			want = append(want, strconv.Itoa(i))
		}

		ds := strconv.Itoa(tc.dataSize)

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_Map/serial:" + ds)()

			result := Map(Of(input...), strconv.Itoa).ToSlice()

			if !slices.Equal(result, want) {
				t.Errorf("result is %v, want %v", result, want)
			}
		})

		t.Run("parallel", func(t *testing.T) {
			defer trace("TestStream_Map/parallel:" + ds)()

			result := Map(Of(input...).Parallel(), strconv.Itoa).ToSlice()

			if !slices.Equal(result, want) {
				t.Errorf("result is %v, want %v", result, want)
			}
		})
	}
}

func TestStream_Distinct(t *testing.T) {
	defer trace("TestStream_Distinct")()

	for _, tc := range [...]struct {
		dataSize int
	}{
		{dataSize: 0},
		{dataSize: 1},
		{dataSize: 1000},
	} {
		var input []int
		var want []int

		for i := 0; i < tc.dataSize; i++ {
			input = append(input, i, i, i)
			want = append(want, i)
		}

		ds := strconv.Itoa(tc.dataSize)

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_Distinct/serial:" + ds)()

			result := Distinct(Of(input...)).ToSlice()

			if !slices.Equal(result, want) {
				t.Errorf("result is %v, want %v", result, want)
			}
		})

		t.Run("parallel",
			func(t *testing.T) {
				defer trace("TestStream_Distinct/parallel:" + ds)()

				result := Distinct(Of(input...).Parallel()).ToSlice()

				for i := 1; i < tc.dataSize; i++ {
					if result[i-1] > result[i] {
						t.Errorf("result[%d] is %d, result[%d] is %d",
							i-1, result[i-1], i, result[i])
					}
				}
				if !slices.Equal(result, want) {
					t.Errorf("result is %v, want %v", result, want)
					os.Exit(1)
				}
			})
	}
}

func TestStream_Sorted(t *testing.T) {
	defer trace("TestStream_Sorted")()

	for _, tc := range [...]struct {
		dataSize int
	}{
		{dataSize: 0},
		{dataSize: 1},
		{dataSize: 1000},
	} {
		var data []int
		var want []int

		for i := 0; i < tc.dataSize; i++ {
			data = append(data, tc.dataSize-i-1)
			want = append(want, i)
		}

		ds := strconv.Itoa(tc.dataSize)

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_Sorted/serial:" + ds)()

			result := Of(data...).Sorted(func(t1, t2 int) bool {
				return t1 < t2
			}).ToSlice()

			if !slices.Equal(result, want) {
				t.Errorf("result is %v, want %v", result, want)
			}
		})

		t.Run("parallel", func(t *testing.T) {
			defer trace("TestStream_Sorted/parallel:" + ds)()

			result := Of(data...).Parallel().Sorted(func(t1, t2 int) bool {
				return t1 < t2
			}).ToSlice()

			if !slices.Equal(result, want) {
				t.Errorf("result is %v, want %v", result, want)
			}
		})
	}
}

func TestStream_Peek(t *testing.T) {
	defer trace("TestStream_Peek")()

	for _, tc := range [...]struct {
		dataSize int
	}{
		{dataSize: 0},
		{dataSize: 1},
		{dataSize: 1000},
	} {
		var data []int
		var want []int

		for i := 0; i < tc.dataSize; i++ {
			data = append(data, tc.dataSize-1-i)
			want = append(want, i)
		}

		ds := strconv.Itoa(tc.dataSize)

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_Peek/serial:" + ds)()

			s := Of(data...)

			var lock sync.Mutex
			var peeked []int
			peekFunc := func(t int) {
				lock.Lock()
				peeked = append(peeked, t)
				lock.Unlock()
			}

			result := s.Peek(peekFunc).Sorted(func(t1, t2 int) bool {
				return t1 < t2
			}).ToSlice()
			if !slices.Equal(result, want) {
				t.Errorf("result is %v, want %v", result, want)
			}

			lock.Lock()
			defer lock.Unlock()

			if !slices.Equal(peeked, data) {
				t.Errorf("peeked is %v, want %v", peeked, data)
			}
		})

		t.Run("parallel", func(t *testing.T) {
			defer trace("TestStream_Peek/parallel:" + ds)()

			s := Of(data...).Parallel()

			var lock sync.Mutex
			var peeked []int
			peekFunc := func(t int) {
				lock.Lock()
				peeked = append(peeked, t)
				lock.Unlock()
			}

			result := s.Peek(peekFunc).Sorted(func(t1, t2 int) bool {
				return t1 < t2
			}).ToSlice()
			if !slices.Equal(result, want) {
				t.Errorf("result is %v, want %v", result, want)
			}

			lock.Lock()
			defer lock.Unlock()

			if tc.dataSize != 0 && tc.dataSize != 1 && goMaxProcs != 1 {
				if slices.Equal(peeked, data) {
					t.Logf("tc.dataSize = %d", tc.dataSize)
					t.Errorf("peeked is identical to data")
				}
			}
			sort.Slice(peeked, func(i, j int) bool {
				return peeked[i] < peeked[j]
			})
			if !slices.Equal(peeked, want) {
				t.Errorf("peeked is %v, want %v", peeked, want)
			}
		})
	}
}

func TestStream_Limit(t *testing.T) {
	defer trace("TestStream_Limit")()

	for _, tc := range [...]struct {
		dataSize int
		limit    int
	}{
		{dataSize: 0, limit: 1},
		{dataSize: 1, limit: 1},
		{dataSize: 1000, limit: 100},
	} {
		var data []int
		var want []int

		for i := 0; i < tc.dataSize; i++ {
			data = append(data, i)
			if i < tc.limit {
				want = append(want, i)
			}
		}

		ds := strconv.Itoa(tc.dataSize)

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_Limit/serial:" + ds)()

			result := Of(data...).Limit(tc.limit).ToSlice()
			if !slices.Equal(result, want) {
				t.Errorf("result is %v, want %v", result, want)
			}
		})

		t.Run("parallel", func(t *testing.T) {
			defer trace("TestStream_Limit/parallel:" + ds)()

			result := Of(data...).Parallel().Limit(tc.limit).ToSlice()
			if !slices.Equal(result, want) {
				t.Errorf("result is %v, want %v", result, want)
			}
		})
	}
}

func TestStream_Skip(t *testing.T) {
	defer trace("TestStream_Skip")()

	for _, tc := range [...]struct {
		dataSize int
		skip     int
	}{
		{dataSize: 0, skip: 1},
		{dataSize: 1, skip: 1},
		{dataSize: 1000, skip: 100},
	} {
		var data []int
		var want []int

		for i := 0; i < tc.dataSize; i++ {
			data = append(data, i)
			if i >= tc.skip {
				want = append(want, i)
			}
		}

		ds := strconv.Itoa(tc.dataSize)

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_Skip/serial:" + ds)()

			result := Of(data...).Skip(tc.skip).ToSlice()
			if !slices.Equal(result, want) {
				t.Errorf("result is %v, want %v", result, want)
			}
		})

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_Skip/parallel:" + ds)()

			result := Of(data...).Parallel().Skip(tc.skip).ToSlice()
			if !slices.Equal(result, want) {
				t.Errorf("result is %v, want %v", result, want)
			}

		})
	}
}

func TestStream_ToSlice(t *testing.T) {
	defer trace("TestStream_ToSlice")()

	for _, tc := range [...]struct {
		dataSize int
	}{
		{dataSize: 0},
		{dataSize: 1},
		{dataSize: 1000},
	} {
		var data []int
		var want []int

		for i := 0; i < tc.dataSize; i++ {
			data = append(data, i)
			want = append(want, i)
		}

		ds := strconv.Itoa(tc.dataSize)

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_ToSlice/serial:" + ds)()

			result := Of(data...).ToSlice()
			if !slices.Equal(result, want) {
				t.Errorf("result is %v, want %v", result, want)
			}
		})

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_ToSlice/serial:" + ds)()

			result := Of(data...).Parallel().ToSlice()
			if !slices.Equal(result, want) {
				t.Errorf("result is %v, want %v", result, want)
			}
		})
	}
}

func TestStream_Reduce(t *testing.T) {
	defer trace("TestStream_Reduce")()

	for _, tc := range [...]struct {
		dataSize int
	}{
		{dataSize: 0},
		{dataSize: 1},
		{dataSize: 1000},
	} {
		var data []int
		var want int

		for i := 0; i < tc.dataSize; i++ {
			data = append(data, i)
			want += i
		}

		ds := strconv.Itoa(tc.dataSize)

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_Reduce/serial:" + ds)()

			result := Of(data...).Reduce(0, Sum[int])

			if result != want {
				t.Errorf("result is %d, want %d", result, want)
			}
		})

		t.Run("parallel", func(t *testing.T) {
			defer trace("TestStream_Reduce/parallel:" + ds)()

			result := Of(data...).Parallel().Reduce(0, Sum[int])

			if result != want {
				t.Errorf("result is %d, want %d", result, want)
			}
		})
	}
}

func TestStream_ReduceToOptional(t *testing.T) {
	defer trace("TestStream_ReduceToOptional")()

	for _, tc := range [...]struct {
		dataSize int
		present  bool
	}{
		{dataSize: 0, present: false},
		{dataSize: 1, present: true},
		{dataSize: 1000, present: true},
	} {
		var data []int
		var want int

		for i := 0; i < tc.dataSize; i++ {
			data = append(data, i)
			want += i
		}

		ds := strconv.Itoa(tc.dataSize)

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_ReduceOptional/serial:" + ds)()

			result := Of(data...).ReduceToOptional(Sum[int])

			if result.IsPresent() != tc.present {
				t.Errorf("result.IsPresent() is %t, want %t",
					result.IsPresent(), tc.present)
			}

			if tc.present && result.Get() != want {
				t.Errorf("result.Get() is %d, want %d", result.Get(), want)
			}
		})

		t.Run("parallel", func(t *testing.T) {
			defer trace("TestStream_ReduceToOptional/parallel:" + ds)()

			result := Of(data...).Parallel().ReduceToOptional(Sum[int])

			if result.IsPresent() != tc.present {
				t.Errorf("result.IsPresent() is %t, want %t",
					result.IsPresent(), tc.present)
			}

			if tc.present && result.Get() != want {
				t.Errorf("result.Get() is %d, want %d", result.Get(), want)
			}

		})

	}
}

func TestStream_Min(t *testing.T) {
	defer trace("TestStream_Min")()

	rand.Seed(time.Now().Unix())

	for _, tc := range [...]struct {
		dataSize int
		present  bool
	}{
		{dataSize: 0, present: false},
		{dataSize: 1, present: true},
		{dataSize: 1000, present: true},
	} {
		var data []int
		var want int
		wantSet := false

		for i := 0; i < tc.dataSize; i++ {
			r := rand.Int()
			data = append(data, r)
			if !wantSet {
				want = r
				wantSet = true
				continue
			}
			if r < want {
				want = r
			}
		}

		ds := strconv.Itoa(tc.dataSize)

		less := func(t1, t2 int) bool { return t1 < t2 }

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_Min/serial:" + ds)()

			min := Of(data...).Min(less)
			if min.IsPresent() != tc.present {
				t.Errorf("min.IsPresent() is %t, want %t",
					min.IsPresent(), tc.present)
			}

			if tc.present && min.Get() != want {
				t.Errorf("min.Get() is %d, want %d", min.Get(), want)
			}
		})

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_Min/parallel:" + ds)()

			min := Of(data...).Parallel().Min(less)
			if min.IsPresent() != tc.present {
				t.Errorf("min.IsPresent() is %t, want %t",
					min.IsPresent(), tc.present)
			}

			if tc.present && min.Get() != want {
				t.Errorf("min.Get() is %d, want %d", min.Get(), want)
			}
		})
	}
}

func TestStream_Max(t *testing.T) {
	defer trace("TestStream_Max")()

	rand.Seed(time.Now().Unix())

	for _, tc := range [...]struct {
		dataSize int
		present  bool
	}{
		{dataSize: 0, present: false},
		{dataSize: 1, present: true},
		{dataSize: 1000, present: true},
	} {
		var data []int
		var want int
		wantSet := false

		for i := 0; i < tc.dataSize; i++ {
			r := rand.Int()
			data = append(data, r)
			if !wantSet {
				want = r
				wantSet = true
				continue
			}
			if r > want {
				want = r
			}
		}

		ds := strconv.Itoa(tc.dataSize)

		less := func(t1, t2 int) bool { return t1 < t2 }

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_Max/serial:" + ds)()

			max := Of(data...).Max(less)
			if max.IsPresent() != tc.present {
				t.Errorf("max.IsPresent() is %t, want %t",
					max.IsPresent(), tc.present)
			}

			if tc.present && max.Get() != want {
				t.Errorf("max.Get() is %d, want %d", max.Get(), want)
			}
		})

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_Max/parallel:" + ds)()

			max := Of(data...).Parallel().Max(less)
			if max.IsPresent() != tc.present {
				t.Errorf("max.IsPresent() is %t, want %t",
					max.IsPresent(), tc.present)
			}

			if tc.present && max.Get() != want {
				t.Errorf("max.Get() is %d, want %d", max.Get(), want)
			}
		})
	}
}

func TestStream_Count(t *testing.T) {
	defer trace("TestStream_Count")()

	for _, tc := range [...]struct {
		dataSize int
	}{
		{dataSize: 0},
		{dataSize: 1},
		{dataSize: 1000},
	} {
		var data []int

		for i := 0; i < tc.dataSize; i++ {
			data = append(data, i)
		}

		ds := strconv.Itoa(tc.dataSize)

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_Count/serial:" + ds)()

			count := Of(data...).Count()
			if count != tc.dataSize {
				t.Errorf("count is %d, want %d", count, tc.dataSize)
			}
		})

		t.Run("parallel", func(t *testing.T) {
			defer trace("TestStream_Count/parallel:" + ds)()

			count := Of(data...).Parallel().Count()
			if count != tc.dataSize {
				t.Errorf("count is %d, want %d", count, tc.dataSize)
			}
		})
	}
}

func TestStream_AnyMatch(t *testing.T) {
	defer trace("TestStream_AnyMatch")()

	for _, tc := range [...]struct {
		dataSize  int
		matchData int
		match     bool
	}{
		{dataSize: 0, matchData: 0, match: false},
		{dataSize: 1, matchData: 0, match: true},
		{dataSize: 1, matchData: 1, match: false},
		{dataSize: 1000, matchData: 100, match: true},
		{dataSize: 1000, matchData: 1000, match: false},
	} {
		var data []int

		for i := 0; i < tc.dataSize; i++ {
			data = append(data, i)
		}

		ds := strconv.Itoa(tc.dataSize)

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_AnyMatch/serial:" + ds)()

			match := Of(data...).AnyMatch(func(t int) bool {
				return t == tc.matchData
			})

			if match != tc.match {
				t.Errorf("match is %t, want %t", match, tc.match)
				t.Errorf("tc: %v", tc)
			}
		})

		t.Run("parallel", func(t *testing.T) {
			defer trace("TestStream_AnyMatch/parallel:" + ds)()

			match := Of(data...).Parallel().AnyMatch(func(t int) bool {
				return t == tc.matchData
			})

			if match != tc.match {
				t.Errorf("match is %t, want %t", match, tc.match)
				t.Errorf("tc: %v", tc)
			}
		})
	}
}

func TestStream_AllMatch(t *testing.T) {
	defer trace("TestStream_AllMatch")()

	for _, tc := range [...]struct {
		dataSize int
		match    bool
	}{
		{dataSize: 0, match: true},
		{dataSize: 1, match: true},
		{dataSize: 1, match: false},
		{dataSize: 1000, match: true},
		{dataSize: 1000, match: false},
	} {
		var data []int

		for i := 0; i < tc.dataSize; i++ {
			data = append(data, i)
		}

		ds := strconv.Itoa(tc.dataSize)

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_AllMatch/serial:" + ds)()

			match := Of(data...).AllMatch(func(t int) bool {
				return tc.match
			})

			if match != tc.match {
				t.Errorf("match is %t, want %t", match, tc.match)
			}
		})

		t.Run("parallel", func(t *testing.T) {
			defer trace("TestStream_AllMatch/parallel:" + ds)()

			match := Of(data...).Parallel().AllMatch(func(t int) bool {
				return tc.match
			})

			if match != tc.match {
				t.Errorf("match is %t, want %t", match, tc.match)
			}
		})
	}
}

func TestStream_NoneMatch(t *testing.T) {
	defer trace("TestStream_NoneMatch")()

	for _, tc := range [...]struct {
		dataSize int
		match    bool
	}{
		{dataSize: 0, match: true},
		{dataSize: 1, match: true},
		{dataSize: 1, match: false},
		{dataSize: 1000, match: true},
		{dataSize: 1000, match: false},
	} {
		var data []int

		for i := 0; i < tc.dataSize; i++ {
			data = append(data, i)
		}

		ds := strconv.Itoa(tc.dataSize)

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_NoneMatch/serial:" + ds)()

			noneMatch := Of(data...).NoneMatch(func(t int) bool {
				return tc.match
			})

			if tc.dataSize == 0 {
				if !noneMatch {
					t.Errorf("noneMatch is false, want true")
				}
				return
			}

			if noneMatch == tc.match {
				t.Errorf("noneMatch is %t, want %t", noneMatch, !tc.match)
			}
		})

		t.Run("parallel", func(t *testing.T) {
			defer trace("TestStream_NoneMatch/parallel:" + ds)()

			noneMatch := Of(data...).Parallel().NoneMatch(func(t int) bool {
				return tc.match
			})

			if tc.dataSize == 0 {
				if !noneMatch {
					t.Errorf("noneMatch is false, want true")
				}
				return
			}

			if noneMatch == tc.match {
				t.Errorf("noneMatch is %t, want %t", noneMatch, !tc.match)
			}
		})
	}
}

func TestStream_FindFirst(t *testing.T) {
	defer trace("TestStream_FindFirst")()

	for _, tc := range [...]struct {
		dataSize int
	}{
		{dataSize: 0},
		{dataSize: 1},
		{dataSize: 1000},
	} {
		var data []int

		for i := 0; i < tc.dataSize; i++ {
			data = append(data, 777-i)
		}

		ds := strconv.Itoa(tc.dataSize)

		t.Run("serial", func(t *testing.T) {
			defer trace("TestStream_FindFirst/serial:" + ds)()

			min := Of(data...).FindFirst()
			if tc.dataSize == 0 {
				if min.IsPresent() {
					t.Errorf("min.IsPresent() is true, want false")
				}
				return
			}

			if min.Get() != 777 {
				t.Errorf("min.Get() is %d, want 777", min.Get())
			}
		})

		t.Run("parallel", func(t *testing.T) {
			defer trace("TestStream_FindFirst/parallel:" + ds)()

			min := Of(data...).Parallel().FindFirst()
			if tc.dataSize == 0 {
				if min.IsPresent() {
					t.Errorf("min.IsPresent() is true, want false")
				}
				return
			}

			if min.Get() != 777 {
				t.Errorf("min.Get() is %d, want 777", min.Get())
			}
		})
	}

}
